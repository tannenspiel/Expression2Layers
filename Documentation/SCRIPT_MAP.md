# Карта скрипта Expression2Layers v1.2

**Версия скрипта:** v1.2  
**Общее количество строк:** 2324  
**Последнее обновление:** 2024

---

## 📋 Содержание

1. [Инициализация и настройки](#1-инициализация-и-настройки)
2. [Создание UI](#2-создание-ui)
3. [Функции обновления UI](#3-функции-обновления-ui)
4. [Обработчики кнопок](#4-обработчики-кнопок)
5. [Снимки состояний](#5-снимки-состояний)
6. [Вспомогательные функции](#6-вспомогательные-функции)
7. [Критические зависимости](#7-критические-зависимости)
8. [Константы для быстрой настройки](#8-константы-для-быстрой-настройки)
9. [Обработчики событий UI](#9-обработчики-событий-ui)
10. [Типичные проблемы и решения](#10-типичные-проблемы-и-решения)
11. [Быстрые ссылки для разработки](#11-быстрые-ссылки-для-разработки)
12. [Архитектурные решения](#12-архитектурные-решения)
13. [Чеклист тестирования](#13-чеклист-тестирования)
14. [Тултипы (Tooltips)](#14-тултипы-tooltips)

---

## 🚨 Критические зависимости

### Функции, от которых зависит основная логика:

- **`applyRevealHack()`** (строка ~2051) - используется в 90% операций раскрытия свойств
  - Зависимости: `deselectAllLayers()`, `quickCollectExpressionProps()`
  - Используется в: `revealExpressionProperty()`, `revealExpressionProperties()`

- **`findPropertyByPath()`** (строка ~1217) - ключевая для работы с кастомными свойствами
  - Зависимости: `isPropertyValid()`
  - Используется в: `saveSnapshotButton.onClick`, `restoreSnapshotButton.onClick`, `revealExpressionProperty()`

- **`updateSnapshotButtonLabels()`** (строка ~312) - критична для корректного отображения UI
  - Зависимости: `fullSnapshotCheckbox.value`, `selectedProperty`
  - Используется в: `languageDropdown.onChange`, `fullSnapshotCheckbox.onClick`, `getPropertyButton.onClick`

- **`safeLayerSelection()`** (строка ~1689) - безопасное выделение слоев с гарантированным порядком
  - Зависимости: `deselectAllLayers()`, `selectProblemLayers()`
  - Используется в: `writeExpressionButton.onClick`, `checkMismatchesButton.onClick`, `restoreSnapshotButton.onClick`

- **`findPropertyPath()`** (строка ~1152) - поиск свойства по пути для обработки несовпадений
  - Зависимости: `isPropertyValid()`
  - Используется в: `writeExpressionButton.onClick`, `checkMismatchesButton.onClick`

---

## ⚙️ Константы для быстрой настройки

### Размеры UI (строки ~243-244):

- `expressionInputWidth = 750` - ширина поля ввода выражения
- `panelButtonWidth = 345` - ширина кнопок в панелях
- Высоты кнопок: 
  - `35px` - стандартные кнопки (getProperty, saveSnapshot, restoreSnapshot, showPropertyExpression, checkMismatches)
  - `75px` - кнопка записи выражения (writeExpressionButton)
  - `50px` - кнопка очистки (clearAllExpressionsButton)

### Тайминги (строки ~2193+):

- `$.sleep(200)` - ожидание обработки ошибок выражений в `applyRevealHack()`
- `$.sleep(300)` - ожидание обновления UI (если используется)

### Оптимизации производительности:

- **Патч 1**: Объединение циклов в `checkMismatchesButton.onClick` - вычисление `maxLayerNameLength` и `maxPathLength` внутри основного цикла (строки ~928-1005)
- **Патч 2**: Функция `safeLayerSelection()` - безопасное выделение слоев (строка ~1689)
- **Патч 3**: Замена вызовов `deselectAllLayers()` + `selectProblemLayers()` на `safeLayerSelection()` в 5 местах
- **Патч 4**: Упрощение `applyRevealHack()` - замена рекурсивной `collectAllExpressionProps()` на `quickCollectExpressionProps()` (строка ~2089)

---

## 🎯 Обработчики событий UI

| Элемент | Событие | Основные вызовы | Строка |
|---------|---------|-----------------|--------|
| `languageDropdown` | `onChange` | `updateSnapshotButtonLabels()`, обновление всех текстов UI | ~226 |
| `fullSnapshotCheckbox` | `onClick` | `updateSnapshotButtonLabels()` | ~348 |
| `showMismatchesCheckbox` | `onClick` | Обновление текста кнопки проверки (`checkMismatchesButton.text`) | ~422 |
| `showOnlyWithExpressionsCheckbox` | `onClick` | Фильтрация в `revealExpressionProperty()` | ~403 |
| `getPropertyButton` | `onClick` | `getDeepestSelectedProperty()`, `updateSnapshotButtonLabels()` | ~358 |
| `writeExpressionButton` | `onClick` | `findPropertyPath()`, `showMismatchDialog()`, `safeLayerSelection()` | ~385 |
| `clearAllExpressionsButton` | `onClick` | `clearExpressionsRecursive()` | ~653 |
| `showPropertyExpressionButton` | `onClick` | `revealExpressionProperty()`, `applyRevealHack()` | ~711 |
| `checkMismatchesButton` | `onClick` | `findPropertyPath()`, создание окна с результатами, `safeLayerSelection()` | ~857 |
| `saveSnapshotButton` | `onClick` | `scanAllExpressions()`, `updateSnapshotButtonLabels()` | ~770 |
| `restoreSnapshotButton` | `onClick` | `restoreExpressionsRecursive()`, `revealExpressionProperties()`, `safeLayerSelection()` | ~931 |
| `selectLayerButton` (в окне несовпадений) | `onClick` | Выделение выбранного слоя из списка | ~1137 |

---

## 🐛 Типичные проблемы и решения

### Проблема: Свойства не раскрываются

**Решение:** Проверить `applyRevealHack()` (строка ~2051)
- Убедиться, что команда "Reveal Expression Errors" существует в меню After Effects
- Проверить временные отключения выражений через `quickCollectExpressionProps()`
- Проверить, что `targetProps` содержит корректные свойства
- Убедиться, что `layersToSelect` не пустой

### Проблема: Не находятся кастомные свойства  

**Решение:** Проверить `findPropertyByPath()` (строка ~1217)
- Убедиться в корректности `propertyPath` (массив matchName или индексов)
- Проверить обработку `matchName` vs `name` (используется `matchName` в приоритете)
- Проверить, что свойство существует на слое через `isPropertyValid()`
- Убедиться, что путь не содержит несуществующих индексов

### Проблема: Интерфейс не обновляется

**Решение:** Проверить `updateSnapshotButtonLabels()` (строка ~312)
- Убедиться, что функция вызывается при изменении `fullSnapshotCheckbox.value`
- Проверить, что `selectedProperty` установлен для режима "selectedProperty"
- Проверить обработчик `languageDropdown.onChange` (строка ~226)

### Проблема: Несовпадения не обнаруживаются

**Решение:** Проверить `checkMismatchesButton.onClick` (строка ~857)
- Убедиться, что свойство выбрано из списка
- Проверить работу `findPropertyPath()` для кастомных свойств
- Проверить логику сравнения путей (`result.hasMismatch`)
- Убедиться, что режим работы (`showMismatches`) установлен корректно

### Проблема: Слои не выделяются после операций

**Решение:** Проверить `safeLayerSelection()` (строка ~1689)
- Убедиться, что функция вызывается с корректными параметрами
- Проверить, что `comp` является экземпляром `CompItem`
- Проверить, что `targetLayers` содержит валидные слои
- Убедиться, что `deselectAllLayers()` вызывается перед `selectProblemLayers()`

---

## 🔧 Быстрые ссылки для разработки

### Для добавления нового функционала:

- **[Обработчики кнопок](#4-обработчики-кнопок)** - шаблон добавления новой кнопки
  - Пример: `showPropertyExpressionButton.onClick` (строка ~711)
  - Структура: проверки → валидация → основная логика → обработка ошибок

- **[Вспомогательные функции](#6-вспомогательные-функции)** - утилиты для работы
  - `safeLayerSelection()` - для выделения слоев
  - `isPropertyValid()` - для проверки свойств
  - `findPropertyByPath()` - для поиска свойств

### Для отладки:

- `$.writeln()` выводы находятся в ключевых функциях:
  - `applyRevealHack()` - подробное логирование процесса раскрытия
  - `writeExpressionButton.onClick` - логирование применения выражений
  - `restoreSnapshotButton.onClick` - логирование восстановления снимков

- Кэш выражений: `expressionsCache` (строка ~205) - используется для отката операций
- Глобальное состояние: `expressionSnapshot` (строка ~206) - хранит снимки выражений

### Для оптимизации:

- **Патч 1**: Объединение циклов - вычисление длин внутри основного цикла
- **Патч 2-3**: Использование `safeLayerSelection()` вместо отдельных вызовов
- **Патч 4**: Упрощение сбора свойств - только эффекты, без рекурсии

---

## 🏗️ Архитектурные решения

### Система локализации:

- **Централизованный словарь** `translations` (строки ~6-181)
  - Структура: `translations[LANGUAGE][key]`
  - Поддержка двух языков: RU и EN

- **Функция доступа** `t(key)` (строки ~200-202)
  - Автоматический выбор языка из `LANGUAGE`
  - Fallback на EN, если ключ не найден

- **Динамическое обновление UI** при смене языка
  - Обработчик `languageDropdown.onChange` (строка ~226)
  - Обновление всех текстовых элементов интерфейса
  - Обновление временных окон (например, окно с несовпадениями)

### Управление состоянием:

- **`expressionSnapshot`** (строка ~206) - глобальное состояние снимков
  - Структура: `{ type, propertyName, isBaseProperty, propertyPath, data }`
  - Хранится в памяти во время сессии
  - Поддерживает два режима: "full" и "selectedProperty"

- **`expressionsCache`** (строка ~205) - временный кэш для отката операций
  - Используется при применении выражений
  - Позволяет откатить изменения при отмене операции
  - Очищается после успешного применения или отмены

- **`selectedProperty`** (строка ~204) - текущее выбранное свойство
  - Устанавливается через `getPropertyButton.onClick`
  - Используется для определения пути к кастомным свойствам
  - Валидируется через `isPropertyValid()`

### Обработка ошибок:

- **Единый паттерн**: try-catch с `$.writeln()` и пользовательскими alert
  - Логирование ошибок в консоль для отладки
  - Понятные сообщения для пользователя через `alert()`
  - Продолжение работы при некритичных ошибках

- **Выделение проблемных слоев** через `safeLayerSelection()`
  - Гарантированный порядок: сначала снятие выделения, затем выделение проблемных
  - Обработка ошибок при выделении (игнорирование недоступных слоев)
  - Используется во всех операциях, которые могут пропустить слои

### Оптимизации производительности:

- **Объединение циклов** (Патч 1)
  - Вычисление максимальных длин имен и путей внутри основного цикла
  - Устранение дополнительного прохода по массиву `mismatchListItems`

- **Упрощение сбора свойств** (Патч 4)
  - Замена рекурсивной функции на простую проверку эффектов
  - Проверка только основных свойств слоя (эффекты)
  - Уменьшение сложности с O(n²) до O(n)

- **Безопасное выделение слоев** (Патч 2-3)
  - Единая функция вместо множественных вызовов
  - Гарантированный порядок операций
  - Упрощение кода и уменьшение дублирования

---

## ✅ Чеклист тестирования функций

### Основной функционал:

- [ ] Запись выражения в базовые свойства (Position, Scale, Rotation, Opacity, Anchor Point)
- [ ] Запись выражения в кастомные свойства (эффекты)
- [ ] Очистка выражений (рекурсивно)
- [ ] Снимки выражений (оба режима: full и selectedProperty)
- [ ] Раскрытие свойств с выражениями (с фильтром и без)
- [ ] Проверка несовпадений/совпадений свойств

### Граничные случаи:

- [ ] Пустые выражения (удаление существующих)
- [ ] Несовпадения путей к свойствам (диалог выбора действия)
- [ ] Отсутствующие свойства на слоях
- [ ] Смена языка во время работы (обновление всех текстов)
- [ ] Очень длинные имена слоев (50+ символов)
- [ ] Очень длинные пути к свойствам (множественные вложенные эффекты)
- [ ] Работа без выделенных слоев (проверка по всем слоям композиции)

### Производительность:

- [ ] Обработка большого количества слоев (50+)
- [ ] Обработка слоев с множеством эффектов (10+ эффектов на слой)
- [ ] Восстановление полного снимка для большого количества слоев
- [ ] Проверка несовпадений для всех слоев композиции

### UI и UX:

- [ ] Динамическое изменение ширины окна с несовпадениями
- [ ] Синхронизация скроллинга в двух списках (имена слоев и пути)
- [ ] Выделение слоев из списка несовпадений
- [ ] Обновление текстов кнопок при смене режима снимка
- [ ] Обновление текстов при смене языка

---

## 1. Инициализация и настройки

### 1.1 Переключатель языка и словарь переводов
- **Строки:** 2-201
- **Описание:** Настройка системы локализации (RU/EN)
- **Ключевые элементы:**
  - `LANGUAGE` (строка 3) - текущий язык интерфейса
  - `translations` (строки 6-181) - словарь переводов для всех текстов
  - `function t(key)` (строки 200-202) - функция получения перевода

### 1.2 Глобальные переменные
- **Строки:** 204-207
- **Переменные:**
  - `selectedProperty` - выбранное пользовательское свойство
  - `expressionsCache` - кэш для отмены операций
  - `expressionSnapshot` - долгосрочный снимок выражений

### 1.3 Управление окном
- **Строки:** 209-213
- **Описание:** Проверка, не открыто ли окно уже, и его показ

---

## 2. Создание UI

### 2.1 Основное окно
- **Строки:** 215-217
- **Тип:** `Window("palette")`
- **Размер:** resizeable

### 2.2 Переключатель языка
- **Строки:** 219-237
- **Элементы:**
  - `languageGroup` - группа с переключателем
  - `languageDropdown` - выпадающий список (RU/EN)
  - **Обработчик:** `languageDropdown.onChange` (строка 226)

### 2.3 Поле ввода выражения
- **Строки:** 239-244
- **Элементы:**
  - `expressionGroup` - группа с полем ввода
  - `expressionInput` - многострочное текстовое поле (750x300)

### 2.4 Левая панель
- **Строки:** 252-269
- **Элементы:**
  - `getPropertyButton` - кнопка "Получить кастомное свойство" (360x25)
  - `propertyList` - список свойств (360x150)
  - `writeExpressionButton` - кнопка записи выражения (360x75)
  - `clearAllExpressionsButton` - кнопка очистки выражений (360x50)

### 2.5 Правая панель
- **Строки:** 299-327
- **Элементы:**
  - `rightPanelsGroup` - группа для правой стороны (содержит правую панель и третью панель)
  - `rightPanel` - панель "Expression Snapshots"
  - `fullSnapshotCheckbox` - чекбокс режима FULL Snapshot
  - `saveSnapshotButton` - кнопка сохранения снимка (345x35)
  - `restoreSnapshotButton` - кнопка восстановления снимка (345x35)

### 2.6 Третья панель (под правой панелью)
- **Строки:** 338-363
- **Элементы:**
  - `showPropertyPanel` - панель "Show Selected Property Expression"
  - `showPropertyExpressionButton` - кнопка показа выражения выбранного свойства (345x35)
  - `showOnlyWithExpressionsCheckbox` - чекбокс "Show only properties with expressions" (выровнен по левому краю)
  - По умолчанию чекбокс включен (показывать только свойства с выражениями)

### 2.7 Подвал
- **Строки:** 293-301
- **Элементы:**
  - `footerGroup` - группа с email автора
  - `footerText` - текст "tannenspiel@gmail.com"

### 2.8 Обработчик закрытия окна
- **Строки:** 365-367
- **Функция:** `scriptWindow.onClose`

---

## 3. Функции обновления UI

### 3.1 updateSnapshotButtonLabels()
- **Строки:** 312-346
- **Назначение:** Обновляет текст кнопок сохранения/восстановления снимков в зависимости от режима
- **Параметры:** нет
- **Вызывается из:**
  - `languageDropdown.onChange` (строка 236)
  - `fullSnapshotCheckbox.onClick` (строка 349)
  - `getPropertyButton.onClick` (строка 366)
  - `saveSnapshotButton.onClick` (строки 687, 798)
  - `restoreSnapshotButton.onClick` (строки 941, 1122)

### 3.2 Обработчик изменения чекбокса
- **Строки:** 348-351
- **Функция:** `fullSnapshotCheckbox.onClick`
- **Действие:** Вызывает `updateSnapshotButtonLabels()`

---

## 4. Обработчики кнопок

### 4.1 getPropertyButton.onClick
- **Строки:** 358-367
- **Назначение:** Получает выбранное свойство из композиции и добавляет его в список
- **Проверки:**
  - Активна ли композиция
  - Выбрано ли одно свойство
- **Действия:**
  - Получает свойство через `getDeepestSelectedProperty()`
  - Добавляет в `propertyList` как "Custom: [имя]"
  - Обновляет кнопки снимков

### 4.2 writeExpressionButton.onClick
- **Строки:** 385-614
- **Назначение:** Применяет выражение к выбранным слоям
- **Основной поток:**
  1. Валидация входных данных (композиция, слои, свойство)
  2. Проверка выражения (пустое/не пустое)
  3. Предварительная проверка свойств на слоях
  4. Применение выражения с обработкой ошибок
  5. Обработка несовпадений путей (через `showMismatchDialog`)
  6. Откат при отмене операции
- **Использует:**
  - `findPropertyPath()` - для кастомных свойств
  - `deselectAllLayers()` - для снятия выделения
  - `selectProblemLayers()` - для выделения проблемных слоев
  - `expressionsCache` - для отката изменений

### 4.3 clearAllExpressionsButton.onClick
- **Строки:** 653-707
- **Назначение:** Очищает все выражения у выбранных слоев
- **Действия:**
  - Подтверждение через `confirm()`
  - Рекурсивная очистка через `clearExpressionsRecursive()`
  - Отчет о количестве очищенных слоев

### 4.4 showPropertyExpressionButton.onClick
- **Строки:** ~711-767
- **Назначение:** Раскрывает выбранное свойство с выражениями для выбранных слоев
- **Проверки:**
  - Активна ли композиция
  - Выбраны ли слои
  - Выбрано ли свойство из списка
- **Действия:**
  - Определяет, базовое это свойство или кастомное
  - Для кастомных свойств получает путь из `selectedProperty`
  - Вызывает `revealExpressionProperty()` для раскрытия свойств
- **Режимы работы (зависит от чекбокса `showOnlyWithExpressionsCheckbox`):**
  - **Включен:** Показывает только свойства с выражениями (по умолчанию)
  - **Выключен:** Показывает все выбранные свойства, даже без выражений
- **Использует:**
  - `revealExpressionProperty()` - для раскрытия свойств
  - `applyRevealHack()` - хак раскрытия через создание временной ошибки

### 4.5 checkMismatchesButton.onClick
- **Строки:** ~857-1193
- **Назначение:** Проверяет несовпадения или совпадения путей к свойствам между слоями
- **Проверки:**
  - Активна ли композиция
  - Выбрано ли свойство из списка
- **Режимы работы:**
  - **Если слои не выделены:** Проверка выполняется по всем слоям композиции
  - **Если слои выделены:** Проверка выполняется только по выделенным слоям
- **Режим отображения (зависит от чекбокса `showMismatchesCheckbox`):**
  - **Включен:** Ищет и показывает несовпадения (по умолчанию)
  - **Выключен:** Ищет и показывает совпадения
- **Действия:**
  1. Определяет, базовое это свойство или кастомное
  2. Для кастомных свойств получает путь из `selectedProperty`
  3. Для каждого слоя проверяет путь к свойству через `findPropertyPath()`
  4. Собирает слои с несовпадениями/совпадениями
  5. **Оптимизация (Патч 1):** Вычисляет `maxLayerNameLength` и `maxPathLength` внутри основного цикла
  6. Если нет результатов - показывает `alert` и выходит
  7. Если есть результаты - создает немодальное окно с двухколоночным списком
  8. Выделяет проблемные слои через `safeLayerSelection()`
- **Окно с результатами:**
  - Динамическая ширина на основе длин имен и путей
  - Два синхронизированных списка: имена слоев и пути к свойствам
  - Кнопка "Select Layer" для выделения выбранного слоя
  - Отображение ожидаемого пути
- **Использует:**
  - `findPropertyPath()` - для поиска свойств и обнаружения несовпадений
  - `safeLayerSelection()` - для выделения проблемных слоев
- **Оптимизация:** Патч 1 - объединение циклов, устранение дополнительного прохода по массиву

---

## 5. Снимки состояний

### 5.1 saveSnapshotButton.onClick
- **Строки:** 770-929
- **Назначение:** Сохраняет снимок выражений (полный или для выбранного свойства)
- **Режимы работы:**
  - **FULL Snapshot** (если `fullSnapshotCheckbox.value === true`):
    - Сканирует все свойства всех выбранных слоев
    - Использует `scanAllExpressions()` рекурсивно
    - Сохраняет в `expressionSnapshot.data[layerName]`
  - **Selected Property** (по умолчанию):
    - Сохраняет только выбранное свойство
    - Сохраняет путь к свойству для последующего восстановления
- **Структура снимка:**
  ```javascript
  {
    type: "full" | "selectedProperty",
    propertyName: string, // только для selectedProperty
    isBaseProperty: boolean, // только для selectedProperty
    propertyPath: array, // только для кастомных свойств
    propertyPathNames: array, // только для кастомных свойств
    data: {
      [layerName]: {
        expression: string, // для selectedProperty
        // или рекурсивная структура для full
        [propertyName]: string,
        _children: { ... }
      }
    }
  }
  ```

### 5.2 restoreSnapshotButton.onClick
- **Строки:** 931-1283
- **Назначение:** Восстанавливает выражения из снимка
- **Режимы работы:**
  - **FULL Snapshot:**
    - Восстанавливает все выражения через `restoreExpressionsRecursive()`
    - Раскрывает свойства через `revealExpressionProperties()`
  - **Selected Property:**
    - Восстанавливает только выбранное свойство
    - Раскрывает свойство через `revealExpressionProperty()`
- **Проверки:**
  - Наличие слоев в композиции
  - Наличие свойств на слоях
  - Предупреждения о проблемных слоях
- **Действия:**
  - Выделение проблемных слоев
  - Раскрытие свойств с выражениями

---

## 6. Вспомогательные функции

### 6.1 Утилиты для работы со слоями

#### deselectAllLayers(comp)
- **Строки:** 1105-1113
- **Назначение:** Снимает выделение со всех слоев композиции
- **Параметры:**
  - `comp` (CompItem) - композиция
- **Используется в:**
  - `writeExpressionButton.onClick` (строки 562, 590)
  - `applyRevealHack()` (строка 1498)

#### selectProblemLayers(layers)
- **Строки:** 1116-1124
- **Назначение:** Выделяет проблемные слои
- **Параметры:**
  - `layers` (Array) - массив объектов Layer
- **Используется в:**
  - `safeLayerSelection()` (строка ~1695)

#### safeLayerSelection(comp, operationType, targetLayers)
- **Строки:** ~1689-1705
- **Назначение:** Безопасное выделение слоев с гарантированным порядком
- **Параметры:**
  - `comp` (CompItem) - композиция
  - `operationType` (String) - тип операции для логирования (опционально)
  - `targetLayers` (Array) - слои для выделения (опционально)
- **Особенности:**
  - ВСЕГДА сначала снимает выделение со всех слоев через `deselectAllLayers()`
  - ТОЛЬКО если указаны целевые слои - выделяет их через `selectProblemLayers()`
  - Обрабатывает ошибки при выделении (игнорирует недоступные слои)
- **Используется в:**
  - `writeExpressionButton.onClick` (строки ~736, 748)
  - `checkMismatchesButton.onClick` (строка ~1190)
  - `restoreSnapshotButton.onClick` (строки ~1465, 1643)
- **Оптимизация:** Патч 2-3 - заменяет множественные вызовы `deselectAllLayers()` + `selectProblemLayers()`

#### findLayerByName(comp, name)
- **Строки:** 1316-1322
- **Назначение:** Находит слой по имени в композиции
- **Параметры:**
  - `comp` (CompItem) - композиция
  - `name` (String) - имя слоя
- **Возвращает:** Layer или null
- **Используется в:**
  - `restoreSnapshotButton.onClick` (множество мест)

### 6.2 Утилиты для работы со свойствами

#### isPropertyValid(property)
- **Строки:** 1127-1134
- **Назначение:** Безопасная проверка валидности свойства
- **Параметры:**
  - `property` (Property) - свойство для проверки
- **Возвращает:** boolean
- **Используется в:**
  - `saveSnapshotButton.onClick` (строка 715)
  - `findPropertyPath()` (строка 1157)

#### getDeepestSelectedProperty(comp)
- **Строки:** 1136-1144
- **Назначение:** Получает самое глубокое выбранное свойство
- **Параметры:**
  - `comp` (CompItem) - композиция
- **Возвращает:** Property или null (если выбрано не одно свойство)
- **Используется в:**
  - `getPropertyButton.onClick` (строка 349)

#### findPropertyPath(layer, propertyName)
- **Строки:** 1152-1215
- **Назначение:** Находит свойство на слое по пути из `selectedProperty`
- **Параметры:**
  - `layer` (Layer) - слой
  - `propertyName` (String) - имя свойства (с префиксом "Custom: ")
- **Возвращает:** объект с полями:
  - `property` (Property) - найденное свойство
  - `hasMismatch` (boolean) - есть ли несовпадение в пути
  - `mismatchInfo` (String) - информация о несовпадении
  - `originalPath` (Array) - оригинальный путь
  - `foundPath` (Array) - найденный путь
- **Используется в:**
  - `writeExpressionButton.onClick` (строка 428, 488)

#### findPropertyByPath(layer, pathArray)
- **Строки:** 1217-1241
- **Назначение:** Находит свойство по массиву пути (matchName или индекс)
- **Параметры:**
  - `layer` (Layer) - слой
  - `pathArray` (Array) - массив matchName или индексов
- **Возвращает:** Property или null
- **Используется в:**
  - `saveSnapshotButton.onClick` (строка 782)
  - `restoreSnapshotButton.onClick` (строки 1000, 1072)
  - `revealExpressionProperty()` (строка 1348)

### 6.3 Функции для работы со снимками

#### scanAllExpressions(property, snapshotObj)
- **Строки:** 1242-1268
- **Назначение:** Рекурсивно сканирует все свойства и сохраняет выражения в объект снимка
- **Параметры:**
  - `property` (Property) - свойство для сканирования
  - `snapshotObj` (Object) - объект для сохранения данных
- **Структура сохранения:**
  - Прямые выражения: `snapshotObj[property.name] = expression`
  - Дочерние свойства: `snapshotObj._children[childName] = {}`
- **Используется в:**
  - `saveSnapshotButton.onClick` (строка 680)

#### restoreExpressionsRecursive(property, snapshotObj)
- **Строки:** 1269-1294
- **Назначение:** Рекурсивно восстанавливает выражения из снимка
- **Параметры:**
  - `property` (Property) - свойство для восстановления
  - `snapshotObj` (Object) - объект с данными снимка
- **Возвращает:** количество восстановленных выражений
- **Используется в:**
  - `restoreSnapshotButton.onClick` (строка 894)

#### hasRealExpressionsInSnapshot(snapshotObj)
- **Строки:** 1296-1315
- **Назначение:** Проверяет, есть ли реальные выражения в структуре снимка
- **Параметры:**
  - `snapshotObj` (Object) - объект снимка
- **Возвращает:** boolean
- **Используется в:**
  - `restoreSnapshotButton.onClick` (строка 904)

### 6.4 Функции раскрытия свойств

#### revealExpressionProperty(layers, propertyName, isBaseProperty, propertyPath)
- **Строки:** 1454-1517
- **Назначение:** Раскрывает конкретное свойство с выражением для выбранных слоев
- **Параметры:**
  - `layers` (Array) - массив слоев
  - `propertyName` (String) - имя свойства
  - `isBaseProperty` (boolean) - базовое ли это свойство
  - `propertyPath` (Array) - путь для кастомных свойств
- **Особенности:**
  - Проверяет состояние чекбокса `showOnlyWithExpressionsCheckbox`
  - Если чекбокс включен - показывает только свойства с выражениями
  - Если чекбокс выключен - показывает все свойства, даже без выражений
- **Использует:**
  - `applyRevealHack()` - для раскрытия через создание временной ошибки
- **Используется в:**
  - `showPropertyExpressionButton.onClick` (строка 765)
  - `restoreSnapshotButton.onClick` (строка 1206)

#### revealExpressionProperties(layers)
- **Строки:** 1389-1488
- **Назначение:** Раскрывает все свойства с выражениями для выбранных слоев
- **Параметры:**
  - `layers` (Array) - массив слоев
- **Внутренняя функция:**
  - `collectExpressionProps()` - рекурсивный сбор свойств с выражениями
- **Использует:**
  - `applyRevealHack()` - для раскрытия через создание временной ошибки
- **Используется в:**
  - `restoreSnapshotButton.onClick` (строка 925)

#### applyRevealHack(targetProps, layersToSelect)
- **Строки:** ~2051-2220
- **Назначение:** Раскрывает свойства через создание временной ошибки выражения (hack из creativecow.net)
- **Параметры:**
  - `targetProps` (Array) - массив объектов с полями `prop`, `path`, `layer`
  - `layersToSelect` (Array) - массив слоев для выделения
- **Алгоритм:**
  1. Снимает выделение со всех слоев через `deselectAllLayers()`
  2. Временно отключает другие выражения на слоях через `quickCollectExpressionProps()`
  3. Создает временную ошибку "1/0" на целевых свойствах (даже если выражения не было)
  4. Выделяет слои
  5. Выполняет команду "Reveal Expression Errors"
  6. Восстанавливает оригинальные выражения
  7. Восстанавливает отключенные выражения
  8. **Выделение сохраняется** (шаг снятия выделения убран)
- **Используется в:**
  - `revealExpressionProperty()` (строка ~1504)
  - `revealExpressionProperties()` (строка ~1603)
- **Оптимизация:** Патч 4 - замена рекурсивной `collectAllExpressionProps()` на `quickCollectExpressionProps()`

#### quickCollectExpressionProps(layer, targetPropsSet)
- **Строки:** ~2089-2118
- **Назначение:** Быстрый сбор свойств с выражениями для временного отключения (только эффекты)
- **Параметры:**
  - `layer` (Layer) - слой для проверки
  - `targetPropsSet` (Object) - Set целевых свойств для исключения
- **Возвращает:** Array объектов с полями `prop`, `path`, `originalEnabled`
- **Особенности:**
  - Проверяет только основные свойства слоя (эффекты)
  - Не рекурсивная (упрощенная версия `collectAllExpressionProps()`)
  - Исключает целевые свойства из отключения
- **Используется в:**
  - `applyRevealHack()` (строка ~2144)
- **Оптимизация:** Патч 4 - уменьшение сложности с O(n²) до O(n)

### 6.5 Функции очистки

#### clearExpressionsRecursive(property)
- **Строки:** 1757-1772
- **Назначение:** Рекурсивно очищает все выражения у свойства и его дочерних свойств
- **Параметры:**
  - `property` (Property) - свойство для очистки
- **Используется в:**
  - `clearAllExpressionsButton.onClick` (строка 639)

### 6.6 Диалоги

#### showMismatchDialog(layerName, mismatchInfo, originalPath, foundPath, hasMoreLayers)
- **Строки:** 1768-1826
- **Назначение:** Показывает диалог при несовпадении пути к свойству
- **Параметры:**
  - `layerName` (String) - имя слоя
  - `mismatchInfo` (String) - информация о несовпадении
  - `originalPath` (Array) - оригинальный путь
  - `foundPath` (Array) - найденный путь
  - `hasMoreLayers` (boolean) - есть ли еще слои для обработки
- **Возвращает:** объект с полями:
  - `apply` (boolean) - применить ли выражение
  - `applyToAll` (boolean) - применить ли ко всем остальным
  - `abort` (boolean) - отменить ли операцию
- **Используется в:**
  - `writeExpressionButton.onClick` (строка 506)

---

## 🔗 Связи между функциями

### Основной поток записи выражения:
```
writeExpressionButton.onClick
  ├─> findPropertyPath() [для кастомных свойств]
  ├─> showMismatchDialog() [при несовпадении]
  └─> safeLayerSelection() [для проблемных слоев]
      ├─> deselectAllLayers() [всегда сначала]
      └─> selectProblemLayers() [если есть проблемные слои]
```

### Основной поток сохранения снимка:
```
saveSnapshotButton.onClick
  ├─> isPropertyValid() [проверка свойства]
  ├─> findPropertyByPath() [для кастомных свойств]
  ├─> scanAllExpressions() [для FULL режима]
  └─> updateSnapshotButtonLabels() [обновление UI]
```

### Основной поток восстановления снимка:
```
restoreSnapshotButton.onClick
  ├─> findLayerByName() [поиск слоев]
  ├─> findPropertyByPath() [поиск свойств]
  ├─> restoreExpressionsRecursive() [для FULL режима]
  ├─> revealExpressionProperties() [для FULL режима]
  ├─> revealExpressionProperty() [для selectedProperty режима]
  │   └─> applyRevealHack() [раскрытие свойств]
  │       ├─> deselectAllLayers() [снятие выделения]
  │       ├─> quickCollectExpressionProps() [сбор свойств для отключения]
  │       └─> [команда "Reveal Expression Errors"]
  └─> safeLayerSelection() [выделение проблемных]
      ├─> deselectAllLayers() [всегда сначала]
      └─> selectProblemLayers() [если есть проблемные слои]
```

### Основной поток проверки несовпадений:
```
checkMismatchesButton.onClick
  ├─> findPropertyPath() [для каждого слоя]
  ├─> [вычисление maxLayerNameLength и maxPathLength внутри цикла] [Патч 1]
  ├─> [создание окна с результатами]
  └─> safeLayerSelection() [выделение проблемных слоев]
      ├─> deselectAllLayers() [всегда сначала]
      └─> selectProblemLayers() [если есть проблемные слои]
```

---

## 📝 Примечания

1. **Отладочные сообщения:** Множество `$.writeln()` для отладки (не видны пользователю, только в ESTK)

2. **Кэш выражений:** `expressionsCache` используется для отката операций при отмене

3. **Структура снимка:** Поддерживает два режима - полный снимок всех свойств и снимок одного свойства

4. **Раскрытие свойств:** Использует хак через создание временной ошибки выражения, так как в AE нет прямого API для раскрытия свойств

5. **Локализация:** Все пользовательские тексты проходят через функцию `t()`

---

## 🔍 Быстрый поиск по функционалу

- **Получить свойство:** `getPropertyButton.onClick` (строка 358)
- **Записать выражение:** `writeExpressionButton.onClick` (строка 385)
- **Очистить выражения:** `clearAllExpressionsButton.onClick` (строка 653)
- **Показать выражение свойства:** `showPropertyExpressionButton.onClick` (строка ~711)
- **Проверить несовпадения:** `checkMismatchesButton.onClick` (строка ~857)
- **Сохранить снимок:** `saveSnapshotButton.onClick` (строка ~770)
- **Восстановить снимок:** `restoreSnapshotButton.onClick` (строка ~931)
- **Найти свойство по пути:** `findPropertyByPath()` (строка ~1217)
- **Раскрыть свойства:** `applyRevealHack()` (строка ~2051)
- **Безопасное выделение слоев:** `safeLayerSelection()` (строка ~1689)
- **Быстрый сбор свойств:** `quickCollectExpressionProps()` (строка ~2089)

---

---

## 📊 Статистика кода

- **Общее количество строк:** 2324
- **Функций:** ~50+
- **Обработчиков событий:** 12
- **Поддерживаемых языков:** 2 (RU, EN)
- **Режимов работы снимков:** 2 (full, selectedProperty)
- **Режимов проверки несовпадений:** 2 (mismatches, matches)

---

## 💡 Тултипы (Tooltips)

### Функция обновления тултипов

#### updateTooltips()
- **Строки:** ~493-522
- **Назначение:** Обновляет все тултипы UI элементов в зависимости от текущего языка и состояния чекбоксов
- **Параметры:** нет
- **Вызывается из:**
  - Инициализация при создании UI (строка ~525)
  - `languageDropdown.onChange` (строка ~312) - при смене языка
  - `fullSnapshotCheckbox.onClick` (строка ~585) - при изменении режима снимка
  - `showOnlyWithExpressionsCheckbox.onClick` (строка ~456) - при изменении фильтра показа
  - `showMismatchesCheckbox.onClick` (строка ~489) - при изменении режима проверки

### Статические тултипы (не зависят от состояния)

| Элемент | Ключ перевода | Описание |
|---------|---------------|----------|
| `getPropertyButton` | `tooltipGetCustomProperty` | Инструкция по получению кастомного свойства |
| `writeExpressionButton` | `tooltipWriteExpression` | Инструкция по записи выражения |
| `clearAllExpressionsButton` | `tooltipClearAllExpressions` | Инструкция по очистке выражений |
| `saveSnapshotButton` | `tooltipSaveSnapshot` | Описание функции сохранения снимка |
| `restoreSnapshotButton` | `tooltipRestoreSnapshot` | Описание функции восстановления снимка |
| `showPropertyExpressionButton` | `tooltipShowSelectedPropertyExpression` | Инструкция по показу выражений свойства |

### Динамические тултипы (зависят от состояния чекбоксов)

| Элемент | Состояние | Ключ перевода | Описание |
|---------|-----------|---------------|----------|
| `fullSnapshotCheckbox` | Включен | `tooltipFullSnapshotEnabled` | Режим полного снимка всех свойств |
| `fullSnapshotCheckbox` | Выключен | `tooltipFullSnapshotDisabled` | Режим снимка выбранного свойства |
| `showOnlyWithExpressionsCheckbox` | Включен | `tooltipShowOnlyWithExpressionsEnabled` | Показывать только свойства с выражениями |
| `showOnlyWithExpressionsCheckbox` | Выключен | `tooltipShowOnlyWithExpressionsDisabled` | Показывать все выбранные свойства |
| `checkMismatchesButton` | `showMismatchesCheckbox.value === true` | `tooltipCheckPropertyMismatches` | Проверка несовпадений |
| `checkMismatchesButton` | `showMismatchesCheckbox.value === false` | `tooltipCheckPropertyMatches` | Проверка совпадений |
| `showMismatchesCheckbox` | Включен | `tooltipShowMismatchesEnabled` | Показывать несовпадающие свойства |
| `showMismatchesCheckbox` | Выключен | `tooltipShowMismatchesDisabled` | Показывать совпадающие свойства |

### Особенности реализации

- **Двуязычная поддержка:** Все тултипы имеют переводы на RU и EN
- **Динамическое обновление:** Тултипы обновляются при смене языка и изменении состояния чекбоксов
- **Синхронизация:** Тултипы кнопок синхронизированы с текстом кнопок (например, `checkMismatchesButton`)
- **Инициализация:** Тултипы устанавливаются при создании UI через `updateTooltips()`

### Структура переводов

Все тултипы находятся в словаре `translations` (строки ~115-130 для RU, ~217-232 для EN):
- Префикс ключей: `tooltip`
- Названия соответствуют элементам UI: `tooltipGetCustomProperty`, `tooltipWriteExpression`, и т.д.
- Динамические тултипы имеют суффиксы: `Enabled` / `Disabled` или `Mismatches` / `Matches`

---

**Последнее обновление карты:** 2024  
**Версия скрипта:** v1.2  
**Оптимизации:** Патчи 1-4 применены  
**Тултипы:** Добавлены для всех кнопок и чекбоксов

